DATABASE

Add profileImageUrl column to users table
→ ALTER TABLE users ADD COLUMN profileImageUrl VARCHAR(255);


ROUTES

Create or extend your /settings route:

/settings/username (POST)

/settings/password (POST)

/settings/email (POST)

/settings/name (POST)

/settings/profile-picture (POST, multipart/form-data)


VIEWS

Settings page with collapsible rows:

Username

Email

Password

First/Last name

Profile picture upload

Reuse .form-container and .setting-row UI pattern